{
  "adp": "couch",
  "connection": "couch",
  "properties": {
    "email": {
      "type": "string",
      "enum": [
        "lowercase"
      ],
      "unique": true,
      "restriction": "email",
      "description": "Email"
    },
    "first_name": {
      "type": "string",
      "enum": [
        "lowercase"
      ],
      "description": "First Name"
    },
    "last_name": {
      "type": "string",
      "enum": [
        "lowercase"
      ],
      "description": "Last Name."
    },
    "password": {
      "type": "string",
      "description": "This will not be included in the response"
    }
  }
}
